

export interface User {
    createdAt: string;
    email: string;
    emailVerified: string | null;
    favoriteIds: string[];
    hashedPassword: string | null;
    id: string;
    image: string;
    name: string;
    updatedAt: string;
  }
  
  // Example usage:

  